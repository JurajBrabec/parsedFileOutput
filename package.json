{
  "name": "parsed-file-output",
  "version": "1.0.2",
  "description": "Parsed File Output",
  "main": "src/index.js",
  "scripts": {
    "build": "esbuild ./src/index.js --bundle --minify --platform=node --outfile=./dist/parsedFileOutput.js",
    "test": "jest"
  },
  "author": "Juraj Brabec",
  "homepage": "https://github.com/JurajBrabec/parsedFileOutput",
  "license": "ISC",
  "devDependencies": {
    "esbuild": "^0.15.1",
    "jest": "^28.1.0"
  }
}
